To run the code in linux terminal, you need to install some important libraries:
Python 3.6 or later is recommended.
Use these commands to install modules and libraries:
sudo apt install python3-tk
pip install psutil
pip install fpdf
sudo apt install apache2
